export enum Collections {
  COURIER_USERS = "courier-users",
  GOV_USERS = "gov-users",
  ORDERS = "orders",
  ADMINS = "admins",
}

export enum OrderStatus {
  STALE = "stale",
  IN_PROGRESS = "progress",
  DELIVERED = "delivered",
}
